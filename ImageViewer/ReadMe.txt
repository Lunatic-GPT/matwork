When save overlay, the saved data is odata_rs
when adjusting the overlay position with the buttons, only odata_rs is changed
to change the overlay itself, one needs to open the data as a underlay instead and use the reference as overlay

 

